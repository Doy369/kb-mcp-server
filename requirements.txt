# MCP Server（锁 1.x：2.x 把 FastMCP 改名 MCPServer，当前代码基于 1.x API）
mcp<2

# pgvector 生产后端（仅 KB_STORAGE_BACKEND=pgvector 时需要）
psycopg[binary]
pgvector

pydantic
python-dotenv

# 生产嵌入（KB_EMBEDDING_BACKEND=bge 时启用，本地模型数据不出域）
# sentence-transformers

# 离线/开发模式（memory 后端 + dev 嵌入器）零额外依赖，无需 numpy
