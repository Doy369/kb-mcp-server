# secrets
.env
runtime_config.json

# local persistence (memory store dump; contains ingested KB content)
kb_store.json
kb_store_demo.json

# 知识图谱持久化（memory 图后端）+ demo 产物
kb_graph.json
kb_graph_demo.json

# 评测产物（每次重跑重新生成；报告本身入库以便前后对比）
kb_store_eval.json
kb_graph_eval.json

# python
__pycache__/
*.pyc
.venv/
venv/

# deps cache
*.egg-info/

# runtime logs
*.log

# P1-5 审计日志（只追加，内容含问答记录，不入库）
kb_audit.jsonl

# 规模化召回评测：嵌入缓存与中间产物（报告 bench_report_*.json 入库便于对比）
bench_emb_*.npz
*.tmp
kb_graph_bench.json
kb_store_bench.json

# PyInstaller build artifacts (exe 二进制不进仓库)
build/
dist/
*.spec
kb-mcp-server.exe
exe_test.log
